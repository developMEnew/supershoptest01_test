export interface BookItem {
  id: string;
  title: string;
  purchasePrice: number;
  sellingPrice: number;
  quantity: number;
  category: string;
  supplier: string;
  dateAdded: string;
  imageUrl: string;
}